class Coordinate(object):

    def __init__(self, latitude, longitude):
        self.latitude = latitude
        self.longitute = longitude
        #Coordinate(latitude=round(math.degrees(lat3), 2), longitute=round(math.degrees(lon3), 2))